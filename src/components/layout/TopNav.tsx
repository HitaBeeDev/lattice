export default function TopNav() {
  return (
    <header>
      <nav aria-label="Primary" className="flex flex-row justify-between">
        {/* Logo */}
        <div>
          <p className="font-heading">Lattice</p>
        </div>
      </nav>
    </header>
  );
}
